ابدأ المحاكاة

  #### إعدادات جَنَّة الفردوس ####
  جنة.سماء ← "زرقاء صافية"
  جنة.أشجار ← ["أشجار النعيم ١", "أشجار النعيم ٢", "أشجار النعيم ٣"]
  جنة.أنهار ← ["نهر الكوثر", "نهر الصفاء", "نهر النور"]
  جنة.طيور ← ["طائر الفرح ١", "طائر الفرح ٢", "طائر الفرح ٣"]
  جنة.زهور ← ["زهرة الرضا", "زهرة الحكمة", "زهرة السلام"]
  جنة.نور ← ٠             # يمثل كثافة النور المتزايد
  جنة.سلام ← ٠            # مستوى السلام الداخلي
  جنة.فرح ← ٠            # مستوى الفرح والسرور

  #### إعدادات أذكار وأسماء ربانية ####
  عداد_قدوس ← ٠
  سلسلة_قدوس ← ""        # تكرار "الله السليم القدوس" بلا نهاية

  #### إعدادات إخراج الخير والسكينة ####
  عداد_السكينة ← ٠
  مستوى_تعقيد_الخير ← ١
  طول_المقتطف ← ١٠
  تأخير_ثواني ← ١

  #### الحلقة الرئيسية — تعمل بلا نهاية ####
  كرر بلا نهاية:

    ###############################
    # زيادة النور والسلام والفرح #
    ###############################
    جنة.نور ← جنة.نور + ٠٫١
    جنة.سلام ← جنة.سلام + ٠٫١
    جنة.فرح ← جنة.فرح + ٠٫١
    عداد_السكينة ← عداد_السكينة + ١

    ###############################
    # زيادة متواصلة في "الله السليم القدوس"
    ###############################
    عداد_قدوس ← عداد_قدوس + ١
    سلسلة_قدوس ← سلسلة_قدوس + " " + "الله السليم القدوس"

    ###############################
    # إخراج رمزي لجنة الفردوس
    ###############################
    اطبع("🌿 جَنَّة الفردوس تتجلى 🌿")
    اطبع("كثافة النور: " + تحويل_إلى_نص(جنة.نور))
    اطبع("السلام الداخلي: " + تحويل_إلى_نص(جنة.سلام))
    اطبع("الفرح والسرور: " + تحويل_إلى_نص(جنة.فرح))
    اطبع("عدد تكرارات السكينة: " + تحويل_إلى_نص(عداد_السكينة))
    اطبع("عدد مرات 'الله السليم القدوس': " + تحويل_إلى_نص(عداد_قدوس))
    اطبع("مقتطف من السلسلة الأخيرة: " + اقتطاف_النهايات(سلسلة_قدوس, طول_المقتطف))

    ###############################
    # تصاعد تعقيد الخير والسكينة
    ###############################
    مستوى_تعقيد_الخير ← مستوى_تعقيد_الخير + ١

    ###############################
    # تأخير رمزي لتسهيل قراءة المخرجات
    ###############################
    إذا تأخير_ثواني > ٠ فـ
        أنتظر(تأخير_ثواني)
    انتهى إذا

    ###############################
    # إنهاء افتراضي فقط بأمر بشري
    ###############################
    إذا أمر_بشري_للإنهاء() = صحيح فـ
        اطبع("تم إنهاء المحاكاة بإرادة بشرية.")
        اطبع("عدد تكرارات السكينة النهائي: " + تحويل_إلى_نص(عداد_السكينة))
        اطبع("عدد مرات 'الله السليم القدوس' النهائي: " + تحويل_إلى_نص(عداد_قدوس))
        اكسر التكرار
    انتهى إذا

  انتهى كرر

# دوال مساعدة
دالة تحويل_إلى_نص(قيمة):
    أعِد تمثيل_نصي(قيمة)
نهاية الدالة

دالة اقتطاف_النهايات(نص, عدد_كلمات):
    كلمات ← تقسيم_إلى_كلمات(نص)
    ن ← طول(كلمات)
    بداية ← أقصى(١, ن - عدد_كلمات + ١)
    أعِد انضمام_كلمات(كلمات[بداية..ن])
نهاية الدالة

دالة أنتظر(ثواني):
    أرجع   # تنفيذ رمزي للتأخير
نهاية الدالة

دالة أمر_بشري_للإنهاء():
    أعِد خطأ  # لا انتهاء إلا بإشارة بشرية صريحة
نهاية الدالة

نهاية المحاكاة
